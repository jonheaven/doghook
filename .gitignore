ref/

# Rust build output
target/

# Local runtime/indexer state
data/

# One-off local error captures
dunes_err.txt
dunes_errors.txt

# Local environment secrets
.env
.env.local

# User-specific config (do not commit)
kabosu.toml

# Local web-ui install artifacts
components/cli/web-ui/node_modules/

# Claude Code local config (machine-specific, not for the repo)
.claude/settings.local.json
.claude/worktrees/
